---
import BaseLayout from '../layouts/BaseLayout.astro';
import '../styles/global.css';
---

<BaseLayout title="Contact — Avietho Digital">
  <!-- Main container -->
  <main class="max-w-7xl mx-auto px-6 md:px-12 pt-4 pb-12 font-body">

    <!-- HERO / AGENCY BANNER SECTION -->
    <section class="relative overflow-hidden rounded-3xl pt-6 pb-8 px-8 md:pt-8 md:pb-12 md:px-16 mb-12 shadow-2xl backdrop-blur-md">
      <!-- Glow effect backdrop -->
      <div class="absolute -top-24 -right-24 w-96 h-96 rounded-full blur-3xl pointer-events-none"></div>

      <div class="relative z-10 max-w-3xl">
        <!-- HEADER TAGLINE -->
        <div class="inline-flex items-center gap-3 text-xs md:text-sm font-semibold tracking-[0.2em] uppercase text-accent mb-4">
          <span class="w-8 h-[1px] bg-accent/60"></span>
          <span>Advertise · Growth · Strategy · Partnerships</span>
        </div>

        <h1 class="text-4xl sm:text-6xl md:text-7xl font-heading font-bold tracking-tight text-foreground leading-[1.05]">
          Let’s scale your <br/>
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent to-accent-hover italic">digital footprint.</span>
        </h1>

        <p class="mt-4 text-lg md:text-xl text-text-subtle font-heading max-w-xl leading-relaxed">
          Interested in advertising, collaboration, or performance marketing opportunities? Tell us what you are building — the Avietho strategist desk reads every brief, and we respond promptly.
        </p>
      </div>

      <!-- Quick Agency Stats / Contact Strip -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6 pt-8 mt-8 border-border/80">
        <div class="bg-surface/50 border border-border/60 p-4 rounded-2xl flex flex-col justify-between hover:border-accent/40 transition">
          <div class="text-xs uppercase tracking-widest text-text-muted mb-2 font-semibold">Landline</div>
          <a href="tel:+63270001366" class="text-base sm:text-lg font-heading font-bold hover:text-accent truncate">
            +63 2 7000 1366
          </a>
        </div>

        <div class="bg-surface/50 border border-border/60 p-4 rounded-2xl flex flex-col justify-between hover:border-accent/40 transition">
          <div class="text-xs uppercase tracking-widest text-text-muted mb-2 font-semibold">Mobile</div>
          <a href="tel:+6397831987" class="text-base sm:text-lg font-heading font-bold text-foreground hover:text-accent transition truncate">
            +63 9783 1987
          </a>
        </div>

        <div class="bg-surface/50 border border-border/60 p-4 rounded-2xl flex flex-col justify-between hover:border-accent/40 transition">
          <div class="text-xs uppercase tracking-widest text-text-muted mb-2 font-semibold">Growth & Partnerships</div>
          <a href="mailto:marketing.avietho@gmail.com" class="text-sm sm:text-base font-heading font-bold hover:text-accent truncate" title="marketing.avietho@gmail.com">
            marketing.avietho@gmail.com
          </a>
        </div>

        <div class="bg-surface/50 border border-border/60 p-4 rounded-2xl flex flex-col justify-between hover:border-accent/40 transition">
          <div class="text-xs uppercase tracking-widest text-text-muted mb-2 font-semibold">Client Strategy</div>
          <a href="mailto:aviethodigital@gmail.com" class="text-sm sm:text-base font-heading font-bold text-foreground hover:text-accent transition truncate" title="aviethodigital@gmail.com">
            aviethodigital@gmail.com
          </a>
        </div>
      </div>
    </section>

    <!-- MAIN INTERACTIVE SECTION -->
    <section class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-12 items-start">
      
      <!-- LEFT COLUMN: AGENCY BRIEF FORM PANEL -->
      <div class="lg:col-span-7 bg-background-elevated/60 border border-border rounded-3xl p-8 shadow-xl backdrop-blur-md flex flex-col justify-between">
        
        <div>
          <!-- PILL OBJECTIVE SELECTORS WITH INTERACTIVE TOGGLE -->
          <div class="mb-8">
            <div class="inline-flex items-center gap-2 text-xs uppercase tracking-widest font-semibold text-accent mb-4">
              <span class="w-6 h-[1px] bg-accent"></span>
              <span>Inquiry Type</span>
            </div>
            <div class="flex flex-wrap gap-2" id="inquiry-pills">
              <button type="button" class="inquiry-btn active border border-accent bg-accent text-background font-semibold px-4 py-2 rounded-full text-xs tracking-wider uppercase transition shadow-md shadow-accent/20">
                Performance Marketing & Ads
              </button>
              <button type="button" class="inquiry-btn border border-border bg-surface/50 text-text-subtle hover:border-accent hover:text-foreground px-4 py-2 rounded-full text-xs tracking-wider uppercase transition">
                Social Media Strategy
              </button>
              <button type="button" class="inquiry-btn border border-border bg-surface/50 text-text-subtle hover:border-accent hover:text-foreground px-4 py-2 rounded-full text-xs tracking-wider uppercase transition">
                SEO & Content Growth
              </button>
              <button type="button" class="inquiry-btn border border-border bg-surface/50 text-text-subtle hover:border-accent hover:text-foreground px-4 py-2 rounded-full text-xs tracking-wider uppercase transition">
                Digital PR & Influencer Campaigns
              </button>
              <button type="button" class="inquiry-btn border border-border bg-surface/50 text-text-subtle hover:border-accent hover:text-foreground px-4 py-2 rounded-full text-xs tracking-wider uppercase transition">
                Brand & Web Development
              </button>
            </div>
          </div>

          <!-- FORM FIELDS -->
          <form class="space-y-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label class="block text-xs font-semibold uppercase tracking-wider text-text-subtle mb-1">
                  First Name <span class="text-accent">*</span>
                </label>
                <input type="text" class="w-full bg-transparent border-b border-border/80 rounded-none py-2 text-foreground placeholder:text-text-muted/40 focus:border-accent focus:outline-none transition" />
              </div>
              <div>
                <label class="block text-xs font-semibold uppercase tracking-wider text-text-subtle mb-1">
                  Last Name <span class="text-accent">*</span>
                </label>
                <input type="text" class="w-full bg-transparent border-b border-border/80 rounded-none py-2 text-foreground placeholder:text-text-muted/40 focus:border-accent focus:outline-none transition" />
              </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label class="block text-xs font-semibold uppercase tracking-wider text-text-subtle mb-1">
                  Work Email <span class="text-accent">*</span>
                </label>
                <input type="email" class="w-full bg-transparent border-b border-border/80 rounded-none py-2 text-foreground placeholder:text-text-muted/40 focus:border-accent focus:outline-none transition" />
              </div>
              <div>
                <label class="block text-xs font-semibold uppercase tracking-wider text-text-subtle mb-1">Phone Number</label>
                <input type="tel" class="w-full bg-transparent border-b border-border/80 rounded-none py-2 text-foreground placeholder:text-text-muted/40 focus:border-accent focus:outline-none transition" />
              </div>
            </div>

            <div>
              <label class="block text-xs font-semibold uppercase tracking-wider text-text-subtle mb-1">Company / Brand Website</label>
              <input type="text" class="w-full bg-transparent border-b border-border/80 rounded-none py-2 text-foreground placeholder:text-text-muted/40 focus:border-accent focus:outline-none transition" />
            </div>

            <div>
              <label class="block text-xs font-semibold uppercase tracking-wider text-text-subtle mb-1">Your Message</label>
              <textarea rows="3" placeholder="Tell us about your target ROI, current digital presence, timeline, or campaign objectives..." class="w-full bg-transparent border-b border-border/80 rounded-none py-2 text-foreground placeholder:text-text-muted/40 focus:border-accent focus:outline-none transition resize-none"></textarea>
            </div>
          </form>
        </div>

        <!-- FORM FOOTER & SUBMIT BUTTON -->
        <div class="pt-6 mt-6 flex flex-col sm:flex-row items-center justify-between gap-6 border-t border-border/60">
          <span class="text-xs uppercase tracking-wider font-semibold text-text-subtle max-w-xs leading-relaxed">
            By submitting, you agree to our Privacy Policy and Terms of Service.
          </span>
          <button type="submit" class="w-full sm:w-auto bg-gradient-to-r from-accent to-accent-hover text-background font-bold text-xs uppercase tracking-widest px-8 py-4 rounded-xl hover:opacity-90 transition shadow-lg shadow-accent/10">
            Send Message
          </button>
        </div>

      </div>

      <!-- RIGHT COLUMN: HQ & DIRECT CONTACT -->
      <div class="lg:col-span-5 lg:sticky lg:top-20 self-start">
        
        <div id="sticky-sidebar" class="flex flex-col justify-start gap-4 p-3 md:p-5 origin-top transition-all duration-300 ease-out">
          
          <!-- HQ OFFICE -->
          <div class="flex flex-col items-start justify-between text-left">
            <div class="flex flex-col items-start w-full">
              <div class="inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-widest text-accent mb-2.5">
                <span class="w-6 h-[1px] bg-accent"></span>
                <span>Headquarters</span>
              </div>

              <h2 class="text-2xl md:text-[1.7rem] font-heading font-bold text-foreground mb-3 leading-snug">
                Madrigal Business Park, <br/>
                <span class="text-accent italic font-normal">Ayala Alabang.</span>
              </h2>

              <!-- UNIFORM FONT WEIGHT ADDRESS BLOCK -->
              <address class="not-italic text-text-subtle text-sm font-heading leading-relaxed flex flex-col items-start gap-[4.2px] mb-5">
                <span>Admiralty Bldg. 1101</span>
                <span>Alabang-Zapote Rd.</span>
                <span>Madrigal Business Park</span>
                <span>Brgy. Ayala Alabang</span>
                <span>Muntinlupa City, Philippines</span>
              </address>
            </div>

            <div>
              <a href="https://maps.app.goo.gl/KyDMwmYMADs5c5LC7" class="inline-flex items-center gap-2 bg-surface hover:bg-background-card border border-border px-4.5 py-2.5 rounded-xl text-xs font-semibold uppercase tracking-wider text-foreground hover:text-accent transition">
                <span>Google Maps</span>
                <span>↗</span>
              </a>
            </div>
          </div>

          <!-- DIRECT LINES -->
          <div class="space-y-3 flex flex-col justify-center pt-3 mt-1">
            <div class="inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-widest text-accent mb-1.5">
              <span class="w-6 h-[1px] bg-accent"></span>
              <span>Direct Strategy Lines</span>
            </div>

            <div class="space-y-1">
              <div class="group flex justify-between items-center py-2.5 border-b border-border/60 text-sm">
                <span class="text-xs uppercase tracking-wider text-text-muted">Landline</span>
                <a href="tel:+63270001366" class="font-heading text-xs md:text-sm font-normal text-foreground group-hover:font-bold group-hover:text-accent transition-all duration-150">
                  +63 2 7000 1366
                </a>
              </div>

              <div class="group flex justify-between items-center py-2.5 border-b border-border/60 text-sm">
                <span class="text-xs uppercase tracking-wider text-text-muted">Mobile</span>
                <a href="tel:+6397831987" class="font-heading text-xs md:text-sm font-normal text-foreground group-hover:font-bold group-hover:text-accent transition-all duration-150">
                  +63 9783 1987
                </a>
              </div>

              <div class="group flex justify-between items-center py-2.5 border-b border-border/60 text-sm">
                <span class="text-xs uppercase tracking-wider text-text-muted">General</span>
                <a href="mailto:aviethodigital@gmail.com" class="font-heading text-xs md:text-sm font-normal text-foreground group-hover:font-bold group-hover:text-accent transition-all duration-150 truncate max-w-[210px]">
                  aviethodigital@gmail.com
                </a>
              </div>

              <div class="group flex justify-between items-center py-2.5 border-b border-border/60 text-sm">
                <span class="text-xs uppercase tracking-wider text-text-muted">Marketing</span>
                <a href="mailto:marketing.avietho@gmail.com" class="font-heading text-xs md:text-sm font-normal text-foreground group-hover:font-bold group-hover:text-accent transition-all duration-150 truncate max-w-[210px]">
                  marketing.avietho@gmail.com
                </a>
              </div>
            </div>
          </div>

        </div>

      </div>
    </section>

  </main>

</BaseLayout>

<!-- SCRIPT FOR DYNAMIC SCROLL ANIMATION & PILL TOGGLES -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    // 1. Inquiry Pill Selection Handling
    const buttons = document.querySelectorAll('.inquiry-btn');
    const inactiveClasses = ['border-border', 'bg-surface/50', 'text-text-subtle'];
    const activeClasses = ['border-accent', 'bg-accent', 'text-background', 'shadow-md', 'shadow-accent/20'];

    buttons.forEach((btn) => {
      btn.addEventListener('click', () => {
        buttons.forEach((b) => {
          b.classList.remove(...activeClasses);
          b.classList.add(...inactiveClasses);
        });

        btn.classList.remove(...inactiveClasses);
        btn.classList.add(...activeClasses);
      });
    });

    // 2. Smooth Downscaling Scroll Animation for HQ + Direct Comm section
    const sidebar = document.getElementById('sticky-sidebar');
    if (sidebar) {
      window.addEventListener('scroll', () => {
        if (window.scrollY > 150) {
          sidebar.classList.add('scale-95', 'opacity-95');
        } else {
          sidebar.classList.remove('scale-95', 'opacity-95');
        }
      }, { passive: true });
    }
  });
</script>